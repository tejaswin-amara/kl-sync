# OCR.space API key for CAPTCHA auto-solve (Magic Wand feature)
# Get a free key at https://ocr.space/ocrapi (free tier: 25k req/month)
# If unset, falls back to the shared "helloworld" key (rate-limited).
OCR_SPACE_API_KEY=
