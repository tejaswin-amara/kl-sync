# Required in production: encrypts session cookies (any string, keep secret)
SESSION_SECRET=

# Required for captcha auto-solve: get a free key at https://ocr.space/ocrapi
OCR_SPACE_API_KEY=

# Optional: set to 'production' for production mode
NODE_ENV=development
