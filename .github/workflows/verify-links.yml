name: Markdown Link & License Integrity Suite

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:
    inputs:
      check_network:
        description: 'Run live external URL reachability checks'
        required: false
        default: false
        type: boolean
  schedule:
    # Run live network link verification every Monday at 04:00 UTC
    - cron: '0 4 * * 1'

permissions:
  contents: read

concurrency:
  group: verify-links-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Markdown, Links & License Integrity
    runs-on: ubuntu-latest
    permissions:
      contents: read

    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5a1 # v4.1.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript Typecheck
        run: npx tsc --noEmit

      - name: Run Markdown Verifier Test Suite
        run: npx --yes tsx --test tests/verify-links.test.ts tests/challenger-pipeline-adversarial.test.ts

      - name: Run Markdown & Link Verification (Deterministic Offline Mode)
        run: npx --yes tsx scripts/verify-links.ts --offline --strict --report verification-report.json

      - name: Run Live Network Reachability Check (Scheduled or Dispatch only)
        if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && inputs.check_network == true)
        run: npx --yes tsx scripts/verify-links.ts --online --concurrency 8 --timeout 10000

      - name: Upload Verification Report Artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: verification-report-node-${{ matrix.node-version }}
          path: verification-report.json
          if-no-files-found: ignore
